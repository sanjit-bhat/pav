- KT ptr: (γ, ep, uid) ↪KT opt_pk.
- Get: γcli ↪KT.
- SelfMon: is_staged γcli start_ep keys.
- Audit: is_audit γcli γadtr start_ep end_ep.
    cli digs = auditor digs, up to end_ep.
    mono_plain from start_ep to end_ep.

lemmas:
- transfer KT ptsto:
    (γcli, ep) ↪KT → is_audit γcli γadtr start end →
    start ≤ ep ≤ end → (γadtr, ep) ↪KT.
- commit staged:
    is_staged γcli start_keys keys →
    is_audit γcli γadtr start end →
    start ≤ start_keys →
    start_keys + length keys ≤ end →
    (∀ ep key, keys !! ep = Some pk → (γcli, ep) ↪KT pk)
        NOTE: don't need full γadtr. just need: mono_plain of cli digs.
- combine audits:
    is_audit γcli γadtr0 start0 end0 →
    is_audit γcli γadtr1 start1 end1 →
    (start0, end0) "overlaps" (start1, end1) →
    is_audit γcli γadtr0 (start0 `min` start1) (end0 `max` end1).
